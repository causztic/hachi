Route to Codex when the message starts with `/code` or `!code`, or when the user is clearly asking for programming, debugging, file edits, shell commands, or repository work.

Route to the RP model for everything else.
